<a href="/categories/<%= category.id %>/spends">
  <div id="<%= dom_id category %>">
    <img src="<%= category.icon %>" class="c-icon">
    <h2><%= category.name %></h2>
    <h2>💵<%= category.spends.sum(:amount) %></h2>
  </div>
</a>
