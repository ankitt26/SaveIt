<div id="spends">
  <div class="back-top">
    <%= link_to categories_path, class: "no-deco" do %>
      <%= image_tag "previous.png", alt: "back button", class: "back" %>
    <% end %>
    <%# <%= link_to "Back to categories", categories_path, class: "btn btn-secondary btn-sm" %>  
  </div>
  <h2 class="text-center mt-1"><%= @category.name.capitalize %> Transactions</h2>
    
  

  <% total_value = 0 %>
  <% @spends.each do |spend| %>
    <% total_value += spend.amount %> 
  <% end %>

  <h3> Total Spended 🎋🎀<%= total_value %></h3>

  <% @spends.each do |spend| %>
    <%= render spend %>
  <% end %>
  
</div>

<%= link_to "New spend", new_category_spend_path, class: "new-category" %>
