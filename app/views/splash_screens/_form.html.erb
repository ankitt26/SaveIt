<%= form_with(model: splash_screen) do |form| %>
  <% if splash_screen.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(splash_screen.errors.count, "error") %> prohibited this splash_screen from being saved:</h2>

      <ul>
        <% splash_screen.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
